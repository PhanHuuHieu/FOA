SELECT
    *
FROM (
        SELECT      
            row_number() OVER (ORDER BY BirthDD DESC) AS [rownum],
            *
        FROM
            mst_kaiin
) T
WHERE 
    rownum BETWEEN (2) AND (50)
